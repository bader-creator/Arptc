// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  
    url: 'http://vps662142.ovh.net/arptc_cosap/web/app_dev.php/api',
    pathavatar:'http://vps662142.ovh.net/arptc_cosap/web/avatars/',
  // pathfile:'http://vps662142.ovh.net/arptc_cosap/web/uploads//',
  //url: 'http://192.168.11.45/app_dev.php/api',
  //pathavatar:'http://192.168.11.45/avatars/',
  //pathfile:'http://192.168.11.45/uploads/Files/',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
